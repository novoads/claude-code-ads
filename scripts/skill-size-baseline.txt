# Raised by exactly +3 on every entry in PR #94 (pack upgrade system, spec §6).
# Each SKILL.md gained the same three mechanical lines: `metadata: {packVersion}`
# in the frontmatter, plus the pack-version note and its blank separator. All 16
# skills grew by +3, not only these four — `git diff origin/main --numstat --
# '*/SKILL.md'` shows it, and the whole diff contains 3 distinct added lines.
# scripts/check-skill-frontmatter.sh --update-baseline REFUSED the raise, as it
# should; these numbers were written by hand. It is one-time: release.sh rewrites
# the stamp in place from now on. NOTE: --update-baseline regenerates this file
# and will drop these comments.
#
# novoads-api is 759 rather than 761: the +3 stamp above landed on it like every
# other skill, and the silence-reserve mode-scoping rewrite then took the body
# down 2 (758 -> 756 pre-stamp). Recomputed from the file, not from either side
# of the rebase conflict — HEAD's 761 had not seen the shrink and the branch's
# 756 had not seen the stamp, so both were wrong by the other's delta.
#
# Raised again by exactly +3 on pixar-ad when its SKILL.md became a build
# artifact of sections/ (scripts/build-skill-md.py). The three lines are the
# AUTO-GENERATED banner the generator injects under the frontmatter, and they are
# the ONLY bytes that changed: strip the banner and the sha256 equals the file
# that was on main. Written by hand for the same reason as the block above --
# --update-baseline refuses a raise, correctly, and would drop these comments.
# claymation-ad took the same +3 and is not listed because it is still
# under 500 (494 -> 497), which is the guard working as intended: the banner buys
# no headroom, so the next section that pushes it over 500 has to earn a row here
# on its own merits.
#
# Lowered by 1 on pixar-ad (1124 -> 1123) when its description was
# trimmed out of the 900-1024 warn band: the cut clause spanned two lines of the
# folded frontmatter and came back as one. Hand-written for the same reason as
# both blocks above -- --update-baseline regenerates this file and drops these
# comments, and the ratchet only goes down, so recording the smaller number by
# hand is the whole point rather than a shortcut around it.
#
# Lowered by 2 on novoads-api (759 -> 757, 2026-08-15) when the two Seedance
# script-length tables became one duration x cadence table and the length gate
# was folded into the tier ask that already existed. The table replacement freed
# 8 lines and the gate spent 6, so this is a shrink recorded rather than headroom
# banked: the number goes down to what the file actually is, which is what makes
# the next growth attempt fail. Hand-written for the same reason as every block
# above -- --update-baseline regenerates this file and drops these comments.
1123 skills/pixar-ad/SKILL.md
839 skills/clone-video-ad/SKILL.md
757 skills/novoads-api/SKILL.md
510 skills/analyze-video/SKILL.md
